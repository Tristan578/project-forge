{
  "name": "spawnforge",
  "private": true,
  "workspaces": ["packages/*", "apps/*", "web"],
  "engines": { "node": ">=20" },
  "overrides": {
    "dompurify": ">=3.3.2",
    "esbuild": ">=0.25.0",
    "lodash": ">=4.18.1"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
