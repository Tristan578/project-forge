{
  "name": "@project-forge/mcp-server",
  "version": "0.2.0",
  "description": "MCP server for SpawnForge game engine — exposes all editor commands as AI tools",
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ws": "^8.20.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "overrides": {
    "path-to-regexp": "^8.4.0"
  },
  "_comments": {
    "overrides": "path-to-regexp pinned to >=8.4.0 (verified: 8.4.0 via npm ls). Transitive dep via express@5/router@2; older versions had ReDoS vulnerabilities."
  }
}
