# Test Coverage Dashboard
*Generated: 2026-03-21 00:31 UTC*

> Auto-generated by `.claude/skills/testing/scripts/generate-coverage-dashboard.sh`
> Re-run after `npx vitest run --coverage` to refresh numbers.

---

## Web Coverage (vitest)

_Web coverage not available._

To generate web coverage, run:
```bash
cd web && npx vitest run --coverage
```
Then re-run this script.

---

## Engine Coverage (cargo)

_Not available — run Rust coverage to generate:_

```bash
cargo llvm-cov --target wasm32-unknown-unknown --lcov --output-path engine/target/coverage/lcov.info
```

---

## Command Coverage

```
=== SpawnForge Command Coverage Audit ===

--- Configuration ---
INFO: Commands manifest: /Users/tristannolan/project-forge/.claude/worktrees/agent-ab135a09/web/src/data/commands.json
INFO: Integration tests: /Users/tristannolan/project-forge/.claude/worktrees/agent-ab135a09/web/src/__integration__/commands
INFO: Handler tests:     /Users/tristannolan/project-forge/.claude/worktrees/agent-ab135a09/web/src/lib/chat/handlers/__tests__
INFO: Coverage threshold: 0%

--- Extracting Commands ---
Total commands in manifest: 327

--- Scanning Test Files ---

--- Results ---

Command Coverage: 320/327 (97.8%)

Untested commands (7):
  set_custom_wgsl_source
  validate_wgsl
  raycast_query
  generate_pixel_art
  set_pixel_art_palette
  quantize_sprite_colors
  set_simplification_backend

=== Audit complete (threshold: 0% — always passes) ===
```

---

## Uncovered Critical Paths

Files in `src/lib/`, `src/stores/`, `src/hooks/`, and `src/app/api/` with 0% line coverage:

- [ ] _Run vitest coverage to detect uncovered paths_

---

## How to Improve Coverage

1. **Run coverage locally:**
   ```bash
   cd web && npx vitest run --coverage
   bash .claude/skills/testing/scripts/generate-coverage-dashboard.sh
   ```

2. **Generate a regression stub from a Sentry issue:**
   ```bash
   SENTRY_AUTH_TOKEN=your_token \
     bash .claude/skills/testing/scripts/sentry-to-test-stub.sh <issue-id>
   ```

3. **Enforce coverage thresholds (ratchet):**
   ```bash
   bash .claude/skills/testing/scripts/ratchet-coverage.sh
   ```

4. **Check command test coverage:**
   ```bash
   bash .claude/skills/testing/scripts/audit-command-coverage.sh
   ```
