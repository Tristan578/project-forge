---
---

Add regression coverage for rejecting completeness metric values whose unit contradicts the metric dictionary. This test-only change does not alter runtime behavior or require a package release.
