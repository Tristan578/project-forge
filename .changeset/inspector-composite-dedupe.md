---
"web": patch
---

Reverb Zone and Audio inspector sliders and the Reverb Zone size field now use the shared `@spawnforge/ui` design-library controls instead of bespoke local copies. Each slider and vector-axis input carries a properly associated accessible name, so screen-reader users hear a distinct label for every control.
