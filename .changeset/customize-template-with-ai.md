---
"web": minor
---

After a starter template loads, a toast now offers "Customize with AI". Clicking it opens the AI chat with "Change this <template> so that " already typed, so you can describe the change in your own words. Nothing is sent until you press Send, and the token estimate is visible first. If you had already typed something in the chat, the suggestion goes on a new line after it instead of replacing it.
