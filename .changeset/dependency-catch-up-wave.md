---
"web": patch
---

Update the AI SDK and PostHog packages to their latest patch releases: `ai` 7.0.78, `@ai-sdk/react` 4.0.81, `@ai-sdk/anthropic` 4.0.41, and `posthog-js` 1.418.14. All four are patch-level bug-fix releases within the ranges already declared, so behaviour is unchanged apart from the fixes they carry.
