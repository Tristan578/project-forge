---
"web": patch
---

Fail CI closed when the Next.js build, MCP command-parity, or Playwright UI gate is skipped despite its trigger firing, and assert the anti-tamper map covers every job the CI Success aggregate waits on.
