---
"web": patch
---

Place right-side workspace panels predictably when the Inspector is absent: fall back to any other open right-dock panel, then to Dockview's default group, instead of docking inside the 3D Viewport group.
