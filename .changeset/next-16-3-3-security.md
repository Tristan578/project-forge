---
"web": patch
"@spawnforge/docs": patch
---

Bump Next.js from 16.3.2 to 16.3.3 to pick up the fix for GHSA-2xp9-vwfh-vxw4 (arbitrary code execution via the AVIF path in Image Optimization). `eslint-config-next` and `@next/bundle-analyzer` move in lockstep so the three stay on one Next release.
