---
"web": patch
---

Game creation: a design that asks for two mechanics in the same category (walk + swim, enemy waves + hazards) now builds steps for both instead of silently dropping all but the first.
