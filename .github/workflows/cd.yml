name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      promote_to_production:
        description: 'Promote staging to production'
        required: false
        type: boolean
        default: false

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: --cfg=web_sys_unstable_apis

jobs:
  # ---- Quality Gates ----
  # On push to main: CI already ran quality gates on this commit (required to merge).
  # Skip re-running them here to save ~780 CI minutes/month (PF-631).
  # On workflow_dispatch: no prior CI run guaranteed, so run gates unconditionally.
  quality-gates:
    name: Quality Gates
    if: ${{ github.event_name == 'workflow_dispatch' }}
    uses: ./.github/workflows/quality-gates.yml

  # ---- WASM CDN Upload ----
  # On push: reuse the WASM artifact already built by CI on this commit (if available),
  # otherwise build fresh. The artifact name is keyed by SHA in ci.yml uploads.
  # On workflow_dispatch: quality-gates.yml builds and uploads the artifact.
  upload-wasm-cdn:
    name: Upload WASM to R2 CDN
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [quality-gates]
    # quality-gates is skipped on push (CI already ran), so treat skipped as success.
    if: |
      always() &&
      vars.R2_CDN_ENABLED == 'true' &&
      (needs.quality-gates.result == 'success' || needs.quality-gates.result == 'skipped')
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Install AWS CLI
        run: |
          curl -fsSL "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip -q awscliv2.zip
          sudo ./aws/install --update

      - name: Download WASM artifacts
        uses: actions/download-artifact@v8
        with:
          name: wasm-binaries
          path: engine/

      - name: Prepare WASM source directory
        run: |
          mkdir -p wasm-upload/engine-pkg-webgl2
          mkdir -p wasm-upload/engine-pkg-webgpu
          mkdir -p wasm-upload/engine-pkg-webgl2-runtime
          mkdir -p wasm-upload/engine-pkg-webgpu-runtime
          cp engine/pkg-webgl2/* wasm-upload/engine-pkg-webgl2/
          cp engine/pkg-webgpu/* wasm-upload/engine-pkg-webgpu/
          cp engine/pkg-webgl2-runtime/* wasm-upload/engine-pkg-webgl2-runtime/ 2>/dev/null || true
          cp engine/pkg-webgpu-runtime/* wasm-upload/engine-pkg-webgpu-runtime/ 2>/dev/null || true

      - name: Upload to R2
        run: bash scripts/upload-wasm-to-r2.sh
        env:
          WASM_SOURCE_DIR: wasm-upload
          ENGINE_VERSION: ${{ github.sha }}
          R2_ACCOUNT_ID: ${{ secrets.ASSET_R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.ASSET_R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.ASSET_R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ vars.R2_BUCKET || 'spawnforge-engine' }}

  # ---- Stage 1: Deploy to Staging ----
  # Runs automatically after all gates pass on push to main.
  # Staging mirrors production config but receives new code first.
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [quality-gates, upload-wasm-cdn]
    if: |
      always() &&
      vars.VERCEL_DEPLOY_ENABLED == 'true' &&
      github.event.inputs.promote_to_production != 'true' &&
      (needs.quality-gates.result == 'success' || needs.quality-gates.result == 'skipped') &&
      (needs.upload-wasm-cdn.result == 'success' || needs.upload-wasm-cdn.result == 'skipped')
    permissions:
      contents: read
      deployments: write
    outputs:
      url: ${{ steps.deploy.outputs.url }}
    environment:
      name: staging
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Vercel CLI
        run: npm i -g vercel@latest

      - name: Download WASM artifacts
        if: ${{ vars.R2_CDN_ENABLED != 'true' }}
        uses: actions/download-artifact@v8
        with:
          name: wasm-binaries
          path: engine/

      - name: Copy WASM to web/public (self-hosted fallback)
        if: ${{ vars.R2_CDN_ENABLED != 'true' }}
        run: |
          mkdir -p web/public/engine-pkg-webgl2
          mkdir -p web/public/engine-pkg-webgpu
          mkdir -p web/public/engine-pkg-webgl2-runtime
          mkdir -p web/public/engine-pkg-webgpu-runtime
          cp engine/pkg-webgl2/* web/public/engine-pkg-webgl2/
          cp engine/pkg-webgpu/* web/public/engine-pkg-webgpu/
          cp engine/pkg-webgl2-runtime/* web/public/engine-pkg-webgl2-runtime/ 2>/dev/null || true
          cp engine/pkg-webgpu-runtime/* web/public/engine-pkg-webgpu-runtime/ 2>/dev/null || true

      - name: Pull Vercel environment (staging)
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_STAGING_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"

      - name: Build with Vercel
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_STAGING_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel build --token="$VERCEL_TOKEN"

      - name: Deploy to staging
        id: deploy
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_STAGING_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          url=$(vercel deploy --prebuilt --token="$VERCEL_TOKEN")
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "Deployed to staging: $url"

  # ---- Stage 1b: Smoke Test Staging ----
  # Runs after staging deploy to verify the deployment is healthy.
  smoke-staging:
    name: Smoke Test (Staging)
    needs: [deploy-staging]
    if: ${{ needs.deploy-staging.result == 'success' }}
    uses: ./.github/workflows/post-deploy-smoke.yml
    with:
      target_url: ${{ needs.deploy-staging.outputs.url }}

  # ---- Stage 2: Deploy to Production ----
  # Auto-deploys after staging succeeds on push to main.
  # Also triggerable manually via workflow_dispatch with promote_to_production=true.
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [quality-gates, upload-wasm-cdn, deploy-staging]
    if: |
      always() &&
      vars.VERCEL_DEPLOY_ENABLED == 'true' &&
      (
        (needs.deploy-staging.result == 'success' && github.event_name == 'push') ||
        (github.event.inputs.promote_to_production == 'true' &&
         needs.quality-gates.result == 'success' &&
         (needs.upload-wasm-cdn.result == 'success' || needs.upload-wasm-cdn.result == 'skipped'))
      )
    permissions:
      contents: read
      deployments: write
    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Vercel CLI
        run: npm i -g vercel@latest

      - name: Download WASM artifacts
        if: ${{ vars.R2_CDN_ENABLED != 'true' }}
        uses: actions/download-artifact@v8
        with:
          name: wasm-binaries
          path: engine/

      - name: Copy WASM to web/public (self-hosted fallback)
        if: ${{ vars.R2_CDN_ENABLED != 'true' }}
        run: |
          mkdir -p web/public/engine-pkg-webgl2
          mkdir -p web/public/engine-pkg-webgpu
          mkdir -p web/public/engine-pkg-webgl2-runtime
          mkdir -p web/public/engine-pkg-webgpu-runtime
          cp engine/pkg-webgl2/* web/public/engine-pkg-webgl2/
          cp engine/pkg-webgpu/* web/public/engine-pkg-webgpu/
          cp engine/pkg-webgl2-runtime/* web/public/engine-pkg-webgl2-runtime/ 2>/dev/null || true
          cp engine/pkg-webgpu-runtime/* web/public/engine-pkg-webgpu-runtime/ 2>/dev/null || true

      - name: Pull Vercel environment (production)
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Build with Vercel
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel build --prod --token="$VERCEL_TOKEN"

      - name: Deploy to production
        id: deploy
        working-directory: web
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          url=$(vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN")
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "Deployed to production: $url"

  # ---- Stage 2b: Smoke Test Production ----
  # Runs after production deploy to verify the deployment is healthy.
  smoke-production:
    name: Smoke Test (Production)
    needs: [deploy-production]
    if: ${{ needs.deploy-production.result == 'success' }}
    uses: ./.github/workflows/post-deploy-smoke.yml
    with:
      target_url: https://spawnforge.ai
