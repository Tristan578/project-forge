{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "cd web && npm ci --prefer-offline && cd ../mcp-server && npm ci --prefer-offline && echo 'Dependencies installed'",
        "cwd": ".",
        "timeoutSec": 120
      }
    ]
  }
}
