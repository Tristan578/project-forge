{
  "name": "engine-cdn",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serving the public engine bucket. Tests cover the handler and the canonical Wrangler config in local workerd/R2.",
  "scripts": {
    "test": "node --test worker.test.mjs worker.workerd.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.116.0"
  },
  "overrides": {
    "sharp": "0.35.4",
    "undici": "7.29.0"
  }
}
